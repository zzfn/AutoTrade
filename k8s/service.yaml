apiVersion: v1
kind: Service
metadata:
  name: autotrade
  namespace: default
  labels:
    app: autotrade
spec:
  type: ClusterIP  # 使用 LoadBalancer 或 NodePort 可从外部访问
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: autotrade
