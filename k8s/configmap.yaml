apiVersion: v1
kind: ConfigMap
metadata:
  name: autotrade-config
  namespace: default
data:
  # 时区设置
  TZ: "Asia/Shanghai"

  # 服务器配置
  HOST: "0.0.0.0"
  PORT: "8000"

  # Alpaca 配置（非敏感部分）
  ALPACA_PAPER: "True"

  # 环境标识
  AUTOTRADE_ENV: "production"
