# AutoTrade

基于 LumiBot 的量化交易系统，支持传统技术指标策略和 ML 驱动策略。

## 功能特性

- 🚀 **实时交易** - 通过 Alpaca API 进行实盘/模拟盘交易
- 📊 **回测系统** - 使用历史数据验证策略表现
- 🤖 **ML 策略** - 基于 Qlib/LightGBM 的机器学习选股策略
- 🎯 **动量策略** - 传统 SMA 交叉策略
- 🌐 **Web 界面** - 实时监控仪表盘和模型管理

## 快速开始

### 1. 安装依赖

```bash
# 安装 Python 依赖
uv sync
```

### 2. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 填入 Alpaca API 密钥
```

### 3. 启动服务

```bash
# 开发模式
make dev

# 或直接运行
uv run uvicorn autotrade.web_server:app --reload
```

访问 http://localhost:8000 查看仪表盘。

## Qlib ML 策略使用指南

### 架构概述

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Web UI)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ Strategy    │  │ Model       │  │ Rolling Update      │  │
│  │ Selection   │  │ Management  │  │ Trigger             │  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     TradeManager                             │
│  MomentumStrategy ◄──────────────────► QlibMLStrategy       │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│   Data Adapter  ──►  Feature Generator  ──►  ML Model       │
└─────────────────────────────────────────────────────────────┘
```

### 1. 初始化数据

首次使用需要获取历史数据并转换为 Qlib 格式：

```bash
# 获取默认股票池的 2 年数据
uv run python scripts/init_qlib_data.py

# 或指定股票和时间范围
uv run python scripts/init_qlib_data.py \
  --symbols SPY,AAPL,MSFT,GOOGL \
  --days 730
```

### 2. 训练模型

```bash
# 基础训练
uv run python scripts/train_model.py --symbols SPY,AAPL,MSFT

# 使用配置文件
uv run python scripts/train_model.py --config configs/qlib_ml_config.yaml

# Walk-Forward 验证
uv run python scripts/train_model.py --symbols SPY --walk-forward

# 训练后设置为当前模型
uv run python scripts/train_model.py --symbols SPY,AAPL,MSFT --set-current
```

### 3. 通过 Web 界面管理

1. 访问 http://localhost:8000/models
2. 选择策略类型（动量策略 / ML 策略）
3. 配置 ML 策略参数（Top-K、再平衡周期）
4. 选择要使用的模型
5. 可以点击「Rolling 更新」按钮更新模型

### 4. API 端点

| 端点                                | 方法 | 说明               |
| ----------------------------------- | ---- | ------------------ |
| `/api/strategy/config`              | GET  | 获取当前策略配置   |
| `/api/strategy/type`                | POST | 设置策略类型       |
| `/api/strategy/ml_config`           | POST | 设置 ML 策略参数   |
| `/api/models`                       | GET  | 列出所有可用模型   |
| `/api/models/current`               | GET  | 获取当前选择的模型 |
| `/api/models/select`                | POST | 选择要使用的模型   |
| `/api/models/rolling_update`        | POST | 启动 Rolling 更新  |
| `/api/models/rolling_update/status` | GET  | 获取更新状态       |

## 策略说明

### MomentumStrategy（动量策略）

基于简单移动平均线（SMA）交叉的经典策略：

- 短期均线上穿长期均线 → 买入
- 短期均线下穿长期均线 → 卖出
- 支持多标的等权重配置

### QlibMLStrategy（ML 策略）

基于机器学习模型预测的策略：

- 使用 LightGBM 预测未来收益率
- Top-K 排名选股（选择预测分数最高的 K 只股票）
- 定期再平衡
- 支持前端配置和模型热切换

#### 特征工程

采用类似 Qlib Alpha158 的技术指标因子：

- 价格回报率（1/5/10/20 天）
- 移动平均线及斜率
- 波动率（ATR、标准差）
- 成交量因子
- RSI、MACD、布林带等技术指标

## 项目结构

```
autotrade/
├── strategies/           # 交易策略
│   ├── momentum_strategy.py
│   └── qlib_ml_strategy.py
├── research/             # 研究模块
│   ├── data/            # 数据适配
│   ├── features/        # 特征工程
│   └── models/          # 模型训练
├── ui/                   # Web 界面
│   └── templates/
├── trade_manager.py      # 交易管理器
└── web_server.py         # Web 服务器

scripts/
├── init_qlib_data.py     # 数据初始化
└── train_model.py        # 模型训练

configs/
├── universe.yaml         # 股票池配置
└── qlib_ml_config.yaml   # ML 策略配置

models/                   # 训练好的模型
data/qlib/               # Qlib 格式数据
```

## 开发

```bash
# 运行测试
uv run pytest

# 代码格式化
uv run ruff format .
uv run ruff check . --fix
```

## 许可证

MIT
